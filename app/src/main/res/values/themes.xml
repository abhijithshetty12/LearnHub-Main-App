<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LearnHub" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/toolbarcolor</item>
    </style>

    <style name="Theme.LearnHub" parent="Base.Theme.LearnHub" />

    <style name="Theme.LearnHub.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.LearnHub.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.LearnHub.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>